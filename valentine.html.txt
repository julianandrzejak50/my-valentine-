valentine.html
<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<title>Be My Valentine 💘</title>

<style>
  body {
    margin: 0;
    height: 100vh;
    background: pink;
    font-family: "Comic Sans MS", cursive;
    display: flex;
    justify-content: center;
    align-items: center;
    overflow: hidden;
  }

  /* floating hearts */
  .heart {
    position: absolute;
    color: #ff4d6d;
    font-size: 20px;
    animation: float 6s linear infinite;
  }

  @keyframes float {
    from { transform: translateY(100vh); opacity: 1; }
    to { transform: translateY(-10vh); opacity: 0; }
  }

  .box {
    background: white;
    padding: 30px;
    border-radius: 25px;
    text-align: center;
    width: 320px;
    position: relative;
  }

  img {
    width: 120px;
    margin-bottom: 10px;
  }

  h1 {
    margin: 10px 0 20px;
  }

  button {
    padding: 12px 24px;
    font-size: 18px;
    border-radius: 12px;
    border: none;
    cursor: pointer;
    margin: 10px;
  }

  #yes {
    background: #ff4d6d;
    color: white;
    transition: transform 0.2s;
  }

  #no {
    background: #ddd;
    position: absolute;
  }

  #message {
    display: none;
    font-size: 20px;
    margin-top: 20px;
  }
</style>
</head>

<body>

<div class="box" id="box">
  <!-- cute cat image -->
  <img src="https://i.imgur.com/JqEuJ6t.png" alt="cute cat">

  <h1>Will you be my Valentine? 💘</h1>

  <button id="yes">Yes 💕</button>
  <button id="no">No 😭</button>

  <div id="message">
   you wish you could have chosen the no u silly lady 😉. unlucky for you its not that easy to get away from me 😈. I do have to say thank you for staying by my side, supporting me through my smart and stupid decisions. you are my most prized possession and I cannot wait to show you that, and treat you to a nice time 😻. I love you to death Mezaab, forever yours -Julie 
  </div>
</div>

<script>
  const noBtn = document.getElementById("no");
  const yesBtn = document.getElementById("yes");
  const box = document.getElementById("box");
  const message = document.getElementById("message");

  let scale = 1;

  noBtn.addEventListener("mouseover", () => {
    const maxX = box.offsetWidth - noBtn.offsetWidth;
    const maxY = box.offsetHeight - noBtn.offsetHeight;

    noBtn.style.left = Math.random() * maxX + "px";
    noBtn.style.top = Math.random() * maxY + "px";

    scale += 0.1;
    yesBtn.style.transform = `scale(${scale})`;
  });

  yesBtn.addEventListener("click", () => {
    yesBtn.style.display = "none";
    noBtn.style.display = "none";
    message.style.display = "block";
  });

  // create hearts
  setInterval(() => {
    const heart = document.createElement("div");
    heart.className = "heart";
    heart.innerHTML = "💖";
    heart.style.left = Math.random() * 100 + "vw";
    heart.style.fontSize = Math.random() * 20 + 10 + "px";
    document.body.appendChild(heart);

    setTimeout(() => heart.remove(), 6000);
  }, 300);
</script>

</body>
</html>
